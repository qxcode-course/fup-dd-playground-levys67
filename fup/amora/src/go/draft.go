package main

import (
	"bufio"
	"fmt"
	"os"
)

func contarsub(texto, substring string) int {
    cont := 0
    for 
}



func main() {
    texto := ""
    sub := ""
    Scanner := bufio.NewScanner(os.Stdin)
    Scanner.Scan()
    texto = Scanner.Text()
    Scanner.Scan()
    sub = Scanner.Text()
    fmt.Println(texto,sub)
}