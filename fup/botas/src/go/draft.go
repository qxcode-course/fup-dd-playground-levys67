package main
import "fmt"

type bota struct{
    tam int
    pe string
}

func criarvetor(qtd int) []bota {
    mapa := make(map[bota]int)
    for i := 0; i < len(vetor); i++ {
        fmt.Scan(
    }
    return vetor
}


func main() {
     qtd := 0
     fmt.Scan(&qtd)
     vetor := criarvetor(qtd)


    fmt.Println(vetor)
}
