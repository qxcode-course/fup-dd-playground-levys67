package main
import "fmt"
func main() {
    dindin := 0
    sabor := ""
    turno := ""
    fmt.Scan(&dindin, &sabor, &turno)
    lista := make([]string, dindin)
    for i := 0; i < dindin; i++ {
        fmt.Scan(&lista[i])
    }
    mais_pedido := lista[0]
    for _;valor := range lista {
        if valor == 
    }

}
